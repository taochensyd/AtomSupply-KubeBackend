{
  "name": "backend",
  "version": "1.0.0",
  "description": "A backend API endpoint build with NodeJS to listen incoming API call and perform task on the local kubernetes pods.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tao Chen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "json-to-pretty-yaml": "^1.2.2",
    "line-reader": "^0.4.0",
    "node-fetch": "^3.3.0",
    "nodemailer": "^6.8.0",
    "shelljs": "^0.8.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
